cmake_minimum_required(VERSION 3.5)
project(b2_sim)

find_package(ament_cmake REQUIRED)

install(
  DIRECTORY launch worlds
  DESTINATION share/${PROJECT_NAME}
)

install(
  PROGRAMS scripts/load_joint_state_broadcaster.py
  DESTINATION lib/${PROJECT_NAME}
)

ament_package()
